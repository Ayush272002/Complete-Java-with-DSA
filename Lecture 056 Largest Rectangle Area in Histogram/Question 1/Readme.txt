Next smaller element
You are given an array ARR of integers of length N. Your task is to find the next smaller element
for each of thr array elements.

Next smaller element for an array is the first element to the right of that element which has a value
strictly smaller than that element.

If for an array element  the next smaller value doesnt exist, you should print -1 for that
array element.